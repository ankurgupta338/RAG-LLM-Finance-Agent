### Create a Virtual Environment, Activate it and Install all the dependencies
```
conda create -n finance_agent python==3.12
conda activate finance_agent
pip install -e .
```

### Which LLM Model to use?
#### Using locally installed Ollama